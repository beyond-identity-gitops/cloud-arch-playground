apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
metadata:
  namespace: istio-system
  name: istio-default
spec:
  profile: default
  addonComponents:
    kiali:
      enabled: true
    tracing:
      enabled: true
  values:
    kiali:
      prometheusAddr: http://prometheus-server.monitoring